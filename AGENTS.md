# AGENTS.md

## プロジェクト名

Tobu TEG Navigator

## このプロジェクトの目的

Tobu TEG Navigatorは、TEG GH / TEG PM の測定値をもとに、院内プロトコールに沿って解釈と次に検討すべき対応を表示する医療者向けWebアプリである。

このアプリは治療を自動決定しない。
あくまで意思決定支援であり、最終判断は担当医が行う。

---

## 開発方針

- TypeScript + Tailwind CSS + Viteで実装する。
- Reactは使用しない。
- スマホ縦画面を最優先にする。
- 夜間救急外来やICUで使いやすいUIにする。
- 患者情報は一切入力・保存しない。
- OCR機能は実装しない。
- 画像アップロード機能は実装しない。
- カメラ撮影機能は実装しない。
- TEG値は医療者が手入力する。
- 未入力値があっても、入力済み項目だけで部分解釈できるようにする。
- 未入力項目は「未評価」と表示する。
- ルールベースの解釈にする。
- 医学的ルールはコードに直書きせず、JSONまたはlogicファイルに分離する。
- 表現は「投与する」ではなく「検討する」とする。
- 危険な断定を避ける。
- すべての結果画面に「最終判断は担当医」と表示する。
- Created by Kosei Omasa をフッターに残す。

---

## UI方針

- ボタンは大きくする。
- 片手操作しやすい縦並びにする。
- 誤タップを避けるため、主要ボタンには十分な高さを確保する。
- 過剰な装飾は避ける。
- 安全性、視認性、明瞭性を優先する。
- 結果画面では赤カードを最上部に表示し、黄、緑、灰の順に表示する。

---

## 採血管表示ルール

GH/PM選択時および入力画面上部には、必ず採血管カードを表示する。

### TEG GH

- 3.2%クエン酸ナトリウム加採血管
- 青スピッツ
- 凝固検査用スピッツ
- 青いキャップの採血管イラストを表示

### TEG PM

- ヘパリン加採血管
- 緑スピッツ
- Platelet Mapping用スピッツ
- 緑のキャップの採血管イラストを表示

採血管カードは SpecimenTubeCard.ts として実装する。

---

## 結果カード見出しルール

結果カードの見出しは、色ごとに以下の形式で統一する。

- 赤カード：見出しは「【異常】〇〇」
- 黄カード：見出しは「【要判断】〇〇」
- 緑カード：見出しは「【範囲内】〇〇」
- 灰カード：見出しは「【未評価】〇〇」

---

## 色分けルール

- 緑：正常範囲、目標範囲内、明らかな異常なし
- 赤：正常値または目標値から外れる明確な異常、プロトコール上の介入検討閾値
- 黄：Expert opinion、根拠が弱い参考解釈、単独では介入判断にしない項目、MTP de-escalation候補
- 灰：未入力、未評価、該当なし

黄色は多用しない。
黄色は、ADP-MAの解釈、HKH-R単独でのFFP判断、ECMOでのCKH-R 10〜16分、MTP de-escalation候補などに限定する。

---

## 禁止事項

- OCR機能を実装しない。
- 画像アップロード機能を作らない。
- カメラ撮影機能を作らない。
- 患者氏名、患者ID、生年月日、診療録番号などの入力欄を作らない。
- 「必ず投与」「投与せよ」などの断定表現を使わない。
- 医学的判断をブラックボックス化しない。
- サーバー保存を前提にしない。
- ルールを画面コンポーネント内に散在させない。

---

## ファイル構成方針

src/
├─ main.ts
├─ vanillaApp.ts
├─ styles.css
├─ data/
│  ├─ rules-gh.json
│  ├─ rules-pm.json
│  ├─ rules-ecmo.json
│  └─ cartridge-decision.json
├─ components/
│  ├─ SpecimenTubeCard.ts
│  └─ SafetyNotice.ts
└─ logic/
   ├─ interpretGH.ts
   ├─ interpretPM.ts
   ├─ interpretECMO.ts
   ├─ decideCartridge.ts
   └─ validateValues.ts

---

## 実装上の注意

- ルールはJSONとlogicファイルに分離する。
- 入力フォームは単位を明示する。
- 未入力でも解釈ボタンは押せるようにする。
- 未入力項目は「未評価」として表示する。
- 結果カードは重要度順に並べる。
- 安全注意文は全結果画面に表示する。
- Created by Kosei Omasa を削除しない。
